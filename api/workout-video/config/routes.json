{
  "routes": [
    {
      "method": "GET",
      "path": "/workout-videos",
      "handler": "workout-video.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/workout-videos/count",
      "handler": "workout-video.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/workout-videos/:id",
      "handler": "workout-video.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/workout-videos",
      "handler": "workout-video.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/workout-videos/:id",
      "handler": "workout-video.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/workout-videos/:id",
      "handler": "workout-video.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
